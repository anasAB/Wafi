# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Subagent-driven-development scratch (reports, briefs, diffs, ledger)
.superpowers/sdd/
.worktrees/

# ruflo/claude-flow plugin runtime state and telemetry (local, not source)
.claude-flow/
ruvector.db

# Local-only DB backups (may contain PII) - never commit
.backups/

# Supabase CLI local scratch state - regenerated every 'supabase start', may contain local dev secrets
supabase/.temp/
